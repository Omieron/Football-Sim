# Copy to .env before: docker compose up -d --build
#
# cp .env.docker.example .env
#
# Live (Case Insider):
#   Web:          https://caseinsider.omerfarukasil.me
#   API (domain): https://caseinsider.omerfarukasil.me/api/...
#   API (direct): http://165.232.69.83:9011
#
# Self-hosted defaults:
#   Web:          http://YOUR_SERVER_IP:9010
#   API (direct): http://YOUR_SERVER_IP:9011

DB_USER=postgres
DB_PASSWORD=change_me_to_a_strong_password
DB_NAME=league

# Public ports on the host (use 9010+ if lower ports are taken)
HTTP_PORT=9010
BACKEND_PORT=9011
